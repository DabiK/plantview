# PlantView — suivi de la boucle Ralph

## Objectif

App React de visualisation PlantUML 100 % client : liens encodés `/view/<code>`, éditeur minimal `/edit`, aucun backend, aucun serveur PlantUML distant.

## Règles

- Une seule tâche par itération (issue GitHub = tâche). Ne jamais commencer la suivante dans la même itération.
- `pnpm lint && pnpm typecheck && pnpm test && pnpm build` verts avant commit (plus `pnpm test:e2e` à partir de M7).
- Commits atomiques, jamais de build cassé. Rendu local uniquement (aucun appel à plantuml.com/planttext/kroki).
- Mettre à jour ce fichier + cocher `PRD.md` + fermer l'issue GitHub (`gh issue close <n>`) à chaque tâche terminée.

## Backlog (issues GitHub ouvertes)

| # | Tâche | Statut |
| --- | --- | --- |
| 1 | M1 — Scaffold Vite + React + TS + Tailwind 4 + structure de base | DONE |
| 2 | M2 — Codec d'URL PlantUML (décoder/encoder) + tests | DONE |
| 3 | M3 — Rendu PlantUML local (@plantuml/core) lazy + sanitize | DONE |
| 4 | M4 — Viewer /view/:code (zoom/pan, dark, export SVG/PNG) | DONE |
| 5 | M5 — Éditeur minimal /edit (CodeMirror, preview live, autosave, erreurs stylées, partage) | DONE |
| 6 | M6 — Page d'accueil / (coller un input, galerie d'exemples, section agents) | DONE |
| 7 | M7 — E2E Playwright + configs de déploiement SPA + doc finale | DONE |
| 8 | M8 — Script d'encodage + skill agent pour générer les liens | DONE |
| 9 | M9 — Style par défaut soigné du rendu (design injecté) | DONE |
| 10 | M10 — Historique local des diagrammes ouverts (localStorage) | TODO |
| 11 | M11 — Déplacer les nœuds (drag) dans le viewer | TODO |

Dépendances : 4 et 5 dépendent de 2 + 3 ; 6 dépend de 2 ; 7 dépend de 4 + 5 + 6 ; 8 dépend de 2 ; 9 dépend de 3 (rendu, indépendant du viewer) ; 10 et 11 dépendent de 4.

## Itérations

### M1 — Scaffold Vite + React + TS + Tailwind 4 + structure de base — DONE

- Base scaffoldée depuis **create-vite@7.1.3** (`template-react-ts`) : l'issue #1 exige `eslint.config.js`, or `create-vite@latest` (v9) est passé à **oxlint** (`.oxlintrc.json`, plus d'ESLint). Décision : rester sur ESLint + flat config, conformément à l'issue (« lint (eslint) »).
- Fichiers copiés sans écraser `README.md`, `AGENTS.md`, `docs/` : `package.json`, `tsconfig*.json`, `vite.config.ts`, `index.html`, `eslint.config.js`, `src/`, `public/vite.svg`. `.gitignore` fusionné (entrées template + `public/plantuml/`, `.ralph/`, `playwright-report/`, `test-results/`, `.vercel/`).
- Dépendances : react-router-dom, @plantuml/core, plantuml-encoder, dompurify, react-zoom-pan-pinch, @uiw/react-codemirror, lucide-react ; dev : tailwindcss, @tailwindcss/vite, @types/plantuml-encoder, vitest.
- pnpm 10 bloquait le postinstall d'esbuild → `pnpm.onlyBuiltDependencies: ["esbuild"]` ajouté dans `package.json`.
- Tailwind 4 branché via le plugin `@tailwindcss/vite` ; `src/index.css` = `@import "tailwindcss";` + reset hauteur plein écran.
- Scripts package.json : `dev`, `build`, `lint`, `typecheck` (`tsc -b`), `test` (`vitest run --passWithNoTests`), `preview`.
- Router : `src/main.tsx` (BrowserRouter) + `src/App.tsx` avec `/` → Home, `/view/:code` → View, `/edit/:code?` → Edit (placeholders sans logique métier). `src/components/` et `src/lib/` créés (`.gitkeep`).
- Vérifs : `pnpm lint && pnpm typecheck && pnpm test && pnpm build` verts. QA navigateur (chrome-devtools) : `/`, `/view/abc`, `/edit/abc`, `/edit` rendus correctement, zéro erreur console.

### M2 — Codec d'URL PlantUML (décoder/encoder) + tests — DONE

- `src/lib/plantuml-encoding.ts` :
  - `decodeDiagram(input)` : extraction (code brut, URL `…/png|svg|txt|uml|view/<code>`, query `?text=`/`?code=`), décodage `plantuml-encoder`, normalisation `\r\n`/`\r` → `\n` (gère les `\r\r` de PlantText), `DiagramDecodeError` si vide/illisible (avec `cause`), message explicite pour URL sans code.
  - `encodeDiagram(source)` : codec standard ; sortie limitée à l'alphabet URL-safe `[0-9A-Za-z_-]` (vérifié par test).
  - `extractDiagramCode(input)` : helper d'extraction réutilisable (M6), retourne `''` si aucun code dans une URL. Détection URL : scheme `://`, `www.`, ou chemin absolu `/…`. Décodage `decodeURIComponent` tolérant (try/catch).
- Tests `src/lib/plantuml-encoding.test.ts` (Vitest env node, 14 tests) : code de référence → lignes non vides `['@startuml', 'Bob -> Alice: Hello!', '@enduml']` ; URL PlantText complète ; round-trip ASCII et accents (`Café à la plage`) ; invalide/vide → `DiagramDecodeError` ; extraction URL plantuml.com/PlantText/PlantView (avec query) ; alphabet du code.
- Vérifié en plus : le bundle navigateur de `plantuml-encoder` (pako) round-trip correctement les accents et reste compatible avec le build node → liens de partage M4/M5 fiables.
- Vérifs : `pnpm lint && pnpm typecheck && pnpm test && pnpm build` verts (build 261 Ko, moteur PlantUML toujours hors bundle). Commit `9f177e9`, issue #2 fermée.

### M3 — Rendu PlantUML local (@plantuml/core) lazy + sanitize — DONE

- `scripts/sync-plantuml-assets.mjs` : copie `plantuml.js`, `viz-global.js`, `emoji.js`, `openiconic.js`, `themes.js` de `node_modules/@plantuml/core/` vers `public/plantuml/` (créé au besoin, exit 1 si le package est absent). `"postinstall"` ajouté dans `package.json`, vérifié via `pnpm install --frozen-lockfile` (le lockfile ne bouge pas).
- `src/lib/render-plantuml.ts` :
  - `renderPlantUml(source, { dark })` → SVG sanitizé. Chargement unique de `viz-global.js` par balise `<script>` classique (singleton, rejet propre + retry possible après échec), puis import dynamique du moteur. `window.PLANTUML_STDLIB_BASE = <base>/plantuml/` pour que themes/emoji/openiconic se chargent depuis nos assets.
  - Les rendus sont **sérialisés par une queue de promesses** : le moteur TeaVM partage son état interne (doc GITHUB_INTEGRATION.md). Garde « latest request » par `requestId` : une requête périmée rejette avec `PlantUmlRenderError` et `superseded: true` (le caller ignore).
  - `parsePlantUmlError(raw)` : extrait `Error line (\d+)` (→ `line`, sinon `null`) et garde la 1re ligne utile comme message court ; fallback « Unknown PlantUML error. ».
  - Sanitize DOMPurify `USE_PROFILES: { svg: true, svgFilters: true }` ; vérifié en navigateur : `xmlns` et `viewBox` conservés.
  - Aucun import statique de `@plantuml/core` (grep sur `src/` vide) ; le moteur n'est pas dans le bundle.
- **Piège Vite 7 résolu** : `import(/* @vite-ignore */ '/plantuml/plantuml.js')` est réécrit en `__vite__injectQuery(url, 'import')` → `?import` sur un fichier de `public/` = 500 « This file is in /public … should not be imported from source code ». `@vite-ignore` ne l'empêche pas. Solution : construire une **URL absolue** via `new URL(`${import.meta.env.BASE_URL}plantuml/plantuml.js`, window.location.href)` ; `injectQuery` ne touche pas les URLs `http(s)://`. Testé en dev **et** en `pnpm preview`.
- **Constat moteur (important pour M5)** : les erreurs de syntaxe ne passent pas par `onError` — le moteur résout un SVG contenant le texte d'erreur (`Syntax Error? (line N)`, « Diagram not supported… »). `onError` ne semble servir qu'aux échecs sévères. M5 devra détecter ces SVG d'erreur (heuristique texte/style) pour afficher le panneau stylé ; `parsePlantUmlError` reste utile si `onError` se déclenche.
- QA navigateur (chrome-devtools, dev puis preview) :
  - `@startuml\nBob -> Alice: Hello!\n@enduml` → SVG avec `<title>Bob</title>`, « Alice », `xmlns`, `viewBox`, ~650 ms après warm-up.
  - `{ dark: true }` → SVG différent du rendu clair.
  - Deux rendus lancés coup sur coup : le 1er rejette `PlantUmlRenderError { superseded: true }`, le 2e (C→D) aboutit.
  - Zéro message console de niveau erreur/warn (le moteur log beaucoup en `console.log`, c'est normal).
  - `dist/plantuml/{plantuml.js,viz-global.js,…}` présents après build ; `dist/assets/*.js` = 261 Ko (< 1 Mo). Le module `render-plantuml` est tree-shaké pour l'instant (aucune route ne l'importe encore) : normal, M4 le branchera.
- Tests : `src/lib/render-plantuml.test.ts` (7 tests) — `parsePlantUmlError` avec/sans ligne, fallback vide, rejet sans DOM (node), échec de chargement de `viz-global.js` + retry (document stubé). Total suite : 20 tests.
- Vérifs : `pnpm lint && pnpm typecheck && pnpm test && pnpm build` verts. Commit `9ffc921`, issue #3 fermée.

### M4 — Viewer /view/:code (zoom/pan, dark, export SVG/PNG) — DONE

- `src/routes/View.tsx` : décodage `:code` → `renderPlantUml`, états loading/ready/error. Pendant un re-rendu (ex. toggle dark) l'ancien SVG reste affiché + badge « Rendering… » (pas de flash de skeleton) ; le skeleton animé n'apparaît qu'au premier rendu. Code invalide → `ErrorPanel` (message + « Open in editor » vers `/edit/<code>`), toolbar conservée avec actions diagramme désactivées.
- `src/components/DiagramCanvas.tsx` : `react-zoom-pan-pinch` v4 (`TransformWrapper` + `TransformComponent`), `fitOnInit`, ref impérative `zoomIn/zoomOut/fit/reset`. Remonté via `key={code}` pour refitter à chaque nouveau diagramme ; le toggle dark ne remonte pas (le zoom est conservé).
- `src/components/Toolbar.tsx` : toolbar flottante en bas (`fixed`, `backdrop-blur`, `overflow-x-auto` mobile), boutons zoom +/−/fit/reset, toggle dark (`aria-pressed`), Download SVG, Download PNG, Copy link, Edit. `aria-label` + `title` sur chaque bouton, feedback transitoire (`role=status`, 2,5 s).
- `src/components/ErrorPanel.tsx` : carte stylée `role=alert`, ligne d'erreur affichée si connue.
- Thème (décidé par DESIGN.md + issue) : `?dark=0|1` > localStorage `plantview:theme` > `prefers-color-scheme`. `src/lib/theme.ts` (pur, testé) + application `<html>.dark` / `data-theme` / `color-scheme` ; variante Tailwind 4 class-based (`@custom-variant dark`). Script inline pre-paint dans `index.html` pour éviter le flash (duplique volontairement la résolution ; la logique reste dans `theme.ts`).
- `src/lib/export.ts` : `downloadSvg`, `downloadPng` (SVG → blob → `Image` → canvas ×2 → `toBlob`, fond blanc/#020617 selon dark, object URLs révoquées). Helpers purs testés : `getSvgDimensions` (width/height ou viewBox) et `withExplicitSvgSize` (ajoute width/height explicites, indispensable pour la rasterisation `<img>`). Piège corrigé : `readAttribute` ne doit pas confondre `stroke-width` et `width`.
- `src/lib/examples.ts` : 4 exemples (sequence, class, activity, usecase) `{ id, title, description, source }`, round-trip codec testé. `src/lib/urls.ts` (`buildViewUrl`) + `src/lib/clipboard.ts` (`copyText` avec fallback textarea/execCommand) ; `copyText` est tolérant : si l'écriture échoue, le viewer affiche « Unable to copy the link ».
- QA navigateur (chrome-devtools, dev **et** `pnpm preview`) :
  - Code de référence → Bob/Alice, fit initial (scale ≈ 3,39), zoom +/− (scale 9,95), pan drag (translation modifiée), fit/reset OK.
  - Thème : système clair → light ; `?dark=1` force dark malgré localStorage light ; `?dark=0` force light malgré localStorage dark ; toggle → light + localStorage `light`.
  - Copy link → « Link copied ». Download PNG → toast + fichier réel 11 460 octets `image/png` (chemin complet testé en interceptant le `<a download>`) ; SVG idem.
  - Code invalide → carte d'erreur stylée + lien éditeur, zéro crash/erreur console.
  - Les 4 exemples `examples.ts` rendus par le moteur : OK, aucun ne contient d'erreur de syntaxe.
  - Mobile 500 px : toolbar compacte, pas de débordement. Zéro message console error/warn.
- Tests : +10 (38 au total) — `theme.test.ts`, `urls.test.ts`, `export.test.ts`, `examples.test.ts`.
- Vérifs : `pnpm lint && pnpm typecheck && pnpm test && pnpm build` verts (bundle 415,5 Ko < 1 Mo, moteur toujours hors bundle).
- **Rappel M5** : les erreurs de syntaxe moteur ne passent pas par `onError` (le moteur résout un SVG contenant « Syntax Error? (line N) »). M4 ne les détecte pas (hors périmètre) : l'éditeur M5 devra les détecter pour afficher le panneau stylé.

### M5 — Éditeur minimal /edit (CodeMirror, preview live, autosave, erreurs stylées, partage) — DONE

- **Détection des SVG d'erreur moteur** (le point laissé ouvert par M4) : `src/lib/diagram-error.ts` (`detectEmbeddedRenderError(svg)`), patterns figés sur des fixtures réelles capturées via chrome-devtools :
  - `Syntax Error?` / `Empty description` + marqueur `[From textarea (line N) ]` → message + ligne ;
  - `Diagram not supported by this release of PlantUML` (directive inconnue, source vide) → message sans ligne ;
  - le marqueur `[From …]` sert de garde anti-faux-positif (une note contenant « Syntax Error? » n'est pas signalée).
  - Intégré dans `renderPlantUml` **après sanitize** : rejet `PlantUmlRenderError` avec `line`. Bénéfice collateral assumé : le viewer M4 affiche désormais le panneau stylé au lieu du SVG rouge du moteur (vérifié : `/view` d'une source invalide → ErrorPanel + « Error at line 2 »).
- `src/lib/plantuml-language.ts` : `StreamLanguage` PlantUML maison (directives `@…`/`!…`, ~80 mots-clés, flèches (`->`, `-->`, `..>`, `<|--`, `o--`, `*--`…), commentaires ligne (`'` en début de ligne) et bloc (`/' … '/` avec état multi-lignes), chaînes, nombres). 9 tests sur le tokenizer (via `StringStream`).
- `src/lib/editor-state.ts` : brouillon localStorage `plantview:draft` = `{ source, updatedAt }` (`parseDraft` défensif, `readDraft`/`writeDraft` tolérants : JSON corrompu → null, quota/private mode → no-op) + `resolveInitialEditorState` (précédence **URL > brouillon > exemple**, `codeError` si le `:code` ne décode pas). 10 tests.
- `src/routes/Edit.tsx` :
  - split view responsive (CodeMirror à gauche, preview à droite ; empilé < `lg`), thème CodeMirror `light`/`dark` suivant le thème global (`theme.ts` réutilisé, toggle dans le header).
  - preview live debouncée 400 ms via `renderPlantUml` (garde superseded de M3) ; le dernier SVG valide reste affiché pendant le rendu (badge « Rendering… ») et en cas d'erreur (estompé sous l'`ErrorPanel`) ; skeleton animé au premier chargement.
  - autosave debouncée 400 ms + indicateur « Saved » discret ; sync URL `history.replaceState` debouncée 900 ms (pas d'entrée d'historique).
  - header (`EditorHeader.tsx`) : lien home, dropdown des 4 exemples (`examples.ts`), Copy view link (presse-papiers + toast), View diagram (navigation in-app), toggle dark. `Toast.tsx` extrait et réutilisé par `Toolbar.tsx` (refactor sans changement visuel).
  - erreurs : `ErrorPanel` (M4) affiche le message + puce « Error at line N » **cliquable** (`onLineClick`, prop optionnelle additive) → scroll/sélection dans CodeMirror (`goToLine`).
  - export SVG/PNG non demandé dans l'éditeur (hors périmètre) ; `DiagramCanvas` réutilisé pour le zoom/pan de la preview.
- Dépendance ajoutée : `@codemirror/language` (StreamLanguage/StringStream ; `@uiw/react-codemirror` ne le ré-exporte pas).
- QA navigateur (chrome-devtools, dev) :
  - `/edit` → exemple par défaut, preview rendue, URL synchronisée ; frappe → preview mise à jour, « Saved », draft localStorage puis rechargement `/edit` → brouillon conservé ; `/edit/<code>` → le code URL gagne sur le brouillon.
  - syntaxe invalide (`frobnicate x`) → ErrorPanel + « Error at line 2 » ; clic → focus éditeur sur la ligne 2 (`.cm-activeLine`).
  - Copy view link → « Link copied » et presse-papiers = `…/view/SoWk…` (le même code que l'URL) ; View diagram → `/view/…` affiche Bob/Alice ; dropdown → exemple class chargé et rendu.
  - Dark/light : toggle → classe `html.dark`, `plantview:theme`, fond CodeMirror et rendu moteur changent.
  - Mobile 500 px (émulation) : header compact, éditeur au-dessus de la preview, pas de débordement. Zéro message console error/warn.
- Tests : +29 (61 au total) — `diagram-error.test.ts`, `plantuml-language.test.ts`, `editor-state.test.ts`.
- Vérifs : `pnpm lint && pnpm typecheck && pnpm test && pnpm build` verts. **Bundle : 832 Ko (269 Ko gzip)** — CodeMirror ajouté au chunk principal ; piste si besoin plus tard : `React.lazy` sur la route Edit (non fait, hors périmètre M5). Commit `cbc1375`, issue #5 fermée.

### M6 — Page d'accueil / (coller un input, galerie d'exemples, section agents) — DONE

- `src/lib/parse-diagram-input.ts` — `parseDiagramInput(input)` en 4 branches, ordre volontaire :
  - vide → `empty` ;
  - `@start…` (regex `/@start[a-z]+/i`, couvre `@startuml`, `@startmindmap`, `@startgantt`…) → `source` ;
  - URL (scheme/`www.`/chemin absolu) → code extrait via `extractDiagramCode` (réutilise M2 : `…/png|svg|txt|uml|view/<code>`, `?text=`) ; URL sans code → repli `source` ;
  - token seul : code seulement si alphabet `[0-9A-Za-z_-]`, longueur ≥ 12 **et décodage réussi** (`decodeDiagram`). Le garde de décodage évite qu'un mot long (ex. `this-is-not-a-valid-plantuml-code`) soit pris pour un code — vérifié manuellement : 45 chaînes aléatoires de 24 caractères ne décodent jamais ;
  - sinon → `source` (fragment sans `@startuml`, cas fréquent au collage).
- `src/routes/Home.tsx` :
  - header (logo, Editor, For agents, toggle dark), hero 2 colonnes (texte + carte de collage) responsive ;
  - carte de collage : textarea, soumission par bouton **ou ⌘/Ctrl+Entrée**, erreur inline `role=alert` + focus si vide (vérifié : `aria-invalid`), `source` → `encodeDiagram` puis navigation `/view/<code>`, `code` → navigation directe ;
  - galerie : 4 cartes depuis `examples.ts`, codes calculés une fois au chargement, chaque carte est un `<Link>` vers `/view/<code>`, icône par type + extrait de source en mono avec fondu (masque CSS) ;
  - section « For agents » : format `/view/<code>`, rappel du codec standard, **URL d'exemple générée à l'exécution** (`encodeDiagram` + `buildViewUrl`, vérifiée : affiche Bob/Alice), commande `printf … | pnpm encode` mentionnée (script M8, pas de dépendance) ;
  - thème identique à View/Edit : `?dark=0|1` > localStorage `plantview:theme` > système, toggle dans le header, `applyThemeToDocument` + `storeTheme`.
  - **Décision — pas de mini-rendus moteur dans la galerie** : l'issue dit « mini-aperçu si simple ». Le moteur TeaVM pèse ~8,6 Mo et bloque le thread principal ; rendre 4 vignettes au chargement de la landing page aurait ajouté plusieurs secondes de jank (et 4 re-rendus à chaque toggle dark). L'aperçu est donc un extrait de source typographié — la page reste légère et le moteur n'est chargé que dans le viewer/l'éditeur.
- Tests : +7 (68 au total) — `parse-diagram-input.test.ts` : vide, `@startuml`/`@startmindmap`, fragment, URL PlantText / plantuml.com `/png` `/svg?query` / PlantView `/view?dark=1`, code de référence seul, token alphabet long non décodable, URL sans code.
- QA navigateur (chrome-devtools, dev **et** `pnpm preview`) :
  - soumission vide → alerte inline + focus textarea ; PlantUML brut via ⌘+Entrée → viewer affiche « Hello from the home page » ; URL PlantText de référence via bouton → Bob/Alice ; clic carte exemple (sequence) → viewer OK ; URL d'exemple « For agents » → Bob/Alice ;
  - `?dark=0` force light ; toggle → `html.dark`, `colorScheme`, localStorage `dark` ;
  - mobile 390 px : header compact, tout empilé, aucun débordement horizontal ; zéro message console error/warn sur toute la session.
- Vérifs : `pnpm lint && pnpm typecheck && pnpm test && pnpm build` verts. Bundle 847,5 Ko (272,9 Ko gzip) — +15 Ko vs M5 (icônes + Home). Commit `076281c`, issue #6 fermée.

### M7 — E2E Playwright + configs de déploiement SPA + doc finale — DONE

- Dépendance `@playwright/test` 1.63 + `pnpm exec playwright install chromium` (Chrome for Testing 153 + headless shell, ~276 Mo dans le cache local). Téléchargement OK, pas de contournement nécessaire.
- `playwright.config.ts` : `testDir ./e2e`, `webServer` = `pnpm dev` sur `http://localhost:5173` (`reuseExistingServer: !CI`, timeout 120 s), `baseURL`, reporter `list`, headless, projet chromium (`devices['Desktop Chrome']`), timeouts larges (test 90 s, expect 30 s) car le moteur (~8,6 Mo) met plusieurs secondes à s'initialiser. En CI : `retries 1`, `workers 1`.
- Script `"test:e2e": "playwright test"` ajouté.
- **Piège évité — Vitest vs specs Playwright** : sans config, `vitest run` utilise l'include par défaut `**/*.test.ts` et aurait ramassé `e2e/*.spec.ts`. Création de `vitest.config.ts` : `exclude: [...configDefaults.exclude, 'e2e/**']`. Vérifié : les 10 fichiers / 68 tests unitaires sont intacts, aucun test e2e n'est exécuté par Vitest.
- ESLint : bloc dédié `playwright.config.ts` + `e2e/**/*.ts` avec `globals.node` (la config globale n'expose que `globals.browser`, or la config Playwright lit `process.env.CI`).
- `e2e/` — 6 tests, tous verts en **15,5 s** headless :
  - `home.spec.ts` : 4 cartes `#examples a[href^="/view/"]` (hrefs valides) ; clic sur la 1re → URL `/view/<code>`.
  - `viewer.spec.ts` : code de référence → `.diagram-svg svg` contient « Bob » et « Alice » ; `?dark=1` → `html[data-theme=dark]` + classe `.dark` ; code invalide (`…code!!!`) → `role=alert` « Unable to render this diagram » + « unable to decode » + lien « Open in editor ».
  - `editor.spec.ts` : localStorage purgé (`addInitScript`) → `/edit` charge l'exemple par défaut → `ControlOrMeta+A` puis saisie de `@startuml Alice -> Bob: Ping @enduml` dans `.cm-content` → le SVG de preview contient « Ping ».
- Déploiement statique SPA (3 configs demandées) :
  - `vercel.json` : rewrite `/(.*)` → `/index.html` (les `rewrites` Vercel s'appliquent après le filesystem, les assets restent servis) ;
  - `netlify.toml` : build `pnpm build`, publish `dist`, redirect `/*` → `/index.html` 200 ;
  - `public/_redirects` : `/* /index.html 200` — vérifié présent dans `dist/_redirects` après build (Vite copie `public/`).
- README final : ajout de `pnpm test:e2e` + note `playwright install chromium`, section **Déploiement** (SPA statique, fallback `index.html` par hébergeur), section **Limitation v1** (pas d'URL image embarquable README/Slack, export SVG/PNG à la place). Routes/dev/build existants conservés.
- Vérifs finales : `pnpm lint && pnpm typecheck && pnpm test && pnpm build` verts + `pnpm test:e2e` 6/6 verts headless. Bundle inchangé (847,5 Ko) : Playwright n'entre pas dans le build.

### M8 — Script d'encodage + skill agent pour générer les liens — DONE

- `scripts/encode.mjs` (ESM, `node:util` `parseArgs`, importe `plantuml-encoder`) :
  - entrée : `--text <source>`, `--file <path>` ou stdin (refus si stdin est un TTY sans input) ; `--text` + `--file` simultanés → erreur ;
  - sortie par défaut : `<base>/view/<code>` ; base = `--base <url>` > `PLANTVIEW_BASE_URL` > `http://localhost:5173`, slashs finaux normalisés ; `--code-only` n'affiche que le code ;
  - `-h`/`--help` (exit 0), toute erreur → message clair sur stderr + exit 1 (source vide, fichier illisible, option inconnue, base vide) ;
  - **bug attrapé par le test** : `PLANTVIEW_BASE_URL=''` (variable vide) écrasait le défaut avec `??` → corrigé en `envValue || DEFAULT_BASE_URL` (seul `--base` explicite peut être vide → erreur).
- `src/lib/encode-script.test.ts` (11 tests) : spawn réel de `node scripts/encode.mjs` (`spawnSync`) — happy path `--text` (URL complète + décodage), pipeline d'acceptance `printf … | pnpm encode`, `--code-only` (alphabet + décodage), `--file` (fichier temporaire), `--base` avec slashs, `PLANTVIEW_BASE_URL` + priorité du flag, entrée vide, `--text`+`--file`, option inconnue, fichier illisible, `--help`. Total suite : **79 tests**.
- Dépendance dev ajoutée : `@types/node` (nécessaire pour typer `node:child_process`/`node:process` dans un test TS ; aucun impact sur le bundle, `window.setTimeout` déjà utilisé partout dans `src`).
- `skill/SKILL.md` : skill agent autonome (frontmatter `name`/`description`) — quand l'utiliser, **règle absolue : jamais d'encodage à la main**, workflow en 3 étapes, exemples `printf … | pnpm encode`, tableau des options, vérification. 100 % cohérent avec le script réel.
- README « Liens pour agents » complété : exemple exact, sources d'entrée, `--base`/`PLANTVIEW_BASE_URL`, `--code-only`, comportement d'erreur, lien vers `skill/SKILL.md` (retrait du « M8 » placeholder).
- QA : `printf '@startuml\n…\n@enduml\n' | pnpm encode` → URL dont le décodage redonne la source ; URL générée ouverte dans le navigateur (chrome-devtools, dev) → titre + Bob/Alice + message affichés, zéro erreur console. `pnpm encode --silent` correctement rejeté (option inconnue transmise par pnpm, exit 1).
- Vérifs : `pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm test:e2e` verts (e2e 6/6). Commit `20865b4`, issue #8 fermée.

### M9 — Style par défaut soigné du rendu (design injecté, pas de thèmes) — DONE

- **Découvertes moteur (spike chrome-devtools, décisives pour l'implémentation)** :
  - Dans un bloc `<style>`, **une propriété par ligne** : deux propriétés sur la même ligne invalident tout le bloc (échec silencieux). `root { BackgroundColor X }` sur une ligne fonctionne ; `usecase { BackgroundColor X LineColor Y }` non.
  - La liste complète des cibles de style est extractible du moteur (table de strings de `plantuml.js`) : `root`, `document`, `node`, `arrow`, `class`, `entity`, `interface`, `component`, `usecase`, `object`, `participant`, `actor`, `boundary`, `control`, `database`, `collections`, `queue`, `stack`, `storage`, `node`, `cloud`, `artifact`, `folder`, `card`, `rectangle`, `person`, `hexagon`, `package`, `state`, `activity`, `diamond`, `activityBar`, `start`, `stop`, `note`, `spot`, `lifeLine`, `groupHeader`…
  - Précédence vérifiée : notre bloc placé **avant** le contenu utilisateur perd contre un `skinparam`/`<style>` utilisateur postérieur (comme exigé). Deux blocs `<style>` : le dernier gagne.
  - `node` n'est **pas** un sélecteur générique qui cascade (testé sur class/component/usecase/sequence/activity/state/object : aucun match) → il faut énumérer les types. `arrow`, lui, marche partout.
  - `document { BackgroundColor … }` peint le fond de page (rect + `background-color` SVG) sans colorer les nœuds, contrairement à `root.BackgroundColor` qui cascade sur les ellipses usecase.
  - Les icônes de classe (`C`/`I`/`E` cerclés) sont des `spot` (fill + `FontColor` de la lettre) ; les points de visibilité (public/privé) gardent leurs couleurs sémantiques.
  - L'option `{ dark }` du moteur est bien écrasée par notre bloc (notre variante gagne).
- `src/lib/default-style.ts` :
  - `buildDefaultStyleBlock({ dark })` : `<style>` (root, document + title/caption/header/footer, arrow, 23 types de nœuds, actor, participant, state, activity, diamond, activityBar, start/stop, note, spot, `sequenceDiagram { lifeLine, groupHeader }`) + repli `skinparam` (DefaultFontColor, ArrowColor, Note*, Actor*, Usecase*, SequenceLifeLineBorderColor, Activity*, State*) pour les éléments non couverts par le style. Palettes slate alignées sur l'UI (dark `#020617`/`#1E293B`/`#475569`/`#E2E8F0`/`#94A3B8`, accents note ambre ; light `#FFFFFF`/`#F8FAFC`/`#CBD5E1`/`#0F172A`/`#64748B`). Police = pile système de l'UI (Tailwind `font-sans`).
  - `injectDefaultStyle(source, { dark })` → `{ source, lineOffset }` : insertion **juste après la première ligne `@start…`** (regex multilignes, préserve `\r\n`), ou en tête s'il n'y a pas de directive ; `lineOffset` = nombre de lignes ajoutées.
  - **Garde « corps vide »** : si la source ne contient que directives/commentaires/lignes vides, aucune injection → le moteur conserve son erreur habituelle « Empty description » (sans la garde, le bloc injecté comptait comme contenu et affichait un diagramme vide).
  - `mapInjectedLineToSourceLine(line, lineOffset)` : mappe les lignes d'erreur moteur vers la source utilisateur (les lignes du bloc injecté → `null`).
- `src/lib/render-plantuml.ts` : injection avant `renderToString` ; les lignes d'erreur (`onError` et SVG d'erreur embarqué) sont mappées avec `lineOffset`. **Régression évitée** : sans cette compensation, une erreur en ligne 3 utilisateur aurait pointé vers la ligne ~90 (bloc injecté) dans l'éditeur.
- Tests : +13 (92 au total) — `default-style.test.ts` : insertion après `@startuml`/`@startmindmap`, tête sans directive, offset identique dans les deux cas, précédence (style/skinparam utilisateur conservés après le bloc), fins de ligne `\r\n`, source utilisateur intacte (retrait du bloc = source d'origine), variantes dark/light, mapping de lignes (null, lignes du bloc), corps vide non injecté.
- QA navigateur (chrome-devtools, dev) :
  - Grille 2×4 des 4 exemples rendus **par le vrai `renderPlantUml`** (dark + light) : aucun SVG d'erreur, rendu cohérent sur sequence / class / activity / usecase ; icônes de classe harmonisées (spots monochromes), notes ambre, acteurs contrastés, barres/diamants d'activité dans la palette.
  - Viewer : class dark plein écran OK (fond `#020617`, cartes `#1E293B`, flèches `#94A3B8`) ; sequence light OK (police système `ui-sans-serif…` et palette présents dans le SVG affiché — donc dans l'export, qui sérialise exactement ce SVG).
  - Précédence réelle via `renderPlantUml` : `<style>` utilisateur `#FF00FF` et `skinparam ArrowColor #00FF00` gagnent bien sur le bloc injecté.
  - Lignes d'erreur : source invalide en ligne 3 → `PlantUmlRenderError.line === 3` (API) et l'éditeur affiche « Error at line 3 » cliquable (vérifié en DOM) ; `@startuml\n@enduml` rejette toujours « Empty description » (ligne 2) ; note/actor en dark vérifiés.
- Vérifs : `pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm test:e2e` verts (e2e 6/6 ; bundle 851,4 Ko, +4 Ko). Commit `95ee949`, issue #9 fermée.

